<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.JasaTataBusana" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorprimary_dark</item>
        <item name="colorPrimaryVariant">@color/colorprimary</item>
        <item name="colorOnPrimary">@color/colordivider</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/coloraccent</item>
        <item name="colorSecondaryVariant">@color/colorsecondary_text</item>
        <item name="colorOnSecondary">@color/coloricons</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.JasaTataBusana.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay"
        parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppTheme.PopupOverlay"
        parent="ThemeOverlay.AppCompat.Light"/>
</resources>